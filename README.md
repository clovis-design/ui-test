# test

Application Angular minimale (front uniquement), sans backend/API.

## Lancer en local

```bash
npm install
npm start
```

Puis ouvrir `http://localhost:4200/`.

## Build

```bash
npm run build
```
